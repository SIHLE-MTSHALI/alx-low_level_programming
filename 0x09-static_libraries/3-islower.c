#include "main.h"

/**
 * _islower - Checks if a character is lowercase
 * @c: Character to check
 *
 * Return: 1 if lowercase, 0 otherwise
 */
int _islower(int c)
{
(void)c; /* This will silence the warning */
return (0);
}

